search:
  paths:
    - "lib/"
data:
  adapter: yaml
  read:
    - "locales/%{locale}.yml"
    - "<%= %x[bundle show vagrant].chomp %>/templates/locales/%{locale}.yml"
  write:
    - "locales/%{locale}.yml"
ignore_unused:
  - "vagrant_parallels.errors.*"
